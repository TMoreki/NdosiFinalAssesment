{
  "name": "ndosi-final-assessment",
  "version": "1.0.0",
  "description": "Playwright UI & API automation framework for Ndosi assessment",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/profile.spec.ts",
    "test:api": "playwright test tests/api.spec.ts",
    "create:avatar": "ts-node fixtures/create-avatar.ts",
    "report": "allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "allure-playwright": "^3.0.0",
    "allure-commandline": "^2.29.0",
    "dotenv": "^16.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
